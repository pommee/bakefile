
# First step to execute
!format:
    cargo fmt

# Lint the codebase, but format first
!lint: format
    cargo lint

# Build a binary in release mode, but run lint firstly
!build-prod: lint
    cargo build --release
